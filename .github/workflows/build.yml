name: Build Status

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    name: Build Status (Python ${{ matrix.python-version }}, Scrapy ${{ matrix.scrapy-version }})
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        include:
          # Lowest supported Scrapy
          - python-version: "3.10"
            scrapy-version: "2.12.*"
          - python-version: "3.11"
            scrapy-version: "2.12.*"
          - python-version: "3.12"
            scrapy-version: "2.12.*"

          # Latest supported Scrapy
          - python-version: "3.10"
            scrapy-version: "<3"
          - python-version: "3.11"
            scrapy-version: "<3"
          - python-version: "3.12"
            scrapy-version: "<3"
          - python-version: "3.13"
            scrapy-version: "<3"
          - python-version: "3.14"
            scrapy-version: "<3"

    steps:
      - uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install "Scrapy==${{ matrix.scrapy-version }}" "requests>=2.31,<3" build
          pip install -e . --no-deps

      - name: Build the package
        run: python -m build

      - name: Check import
        run: python -c "import scrapy_flaresolverr"
